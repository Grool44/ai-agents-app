[phases.setup]
nixPkgs = ["python311Packages.python"]

[phases.install]
cmds = ["python -m pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "cd backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"